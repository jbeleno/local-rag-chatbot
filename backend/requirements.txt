# ---------------- Web framework ----------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---------------- LangChain (RAG orchestration) ----------------
# NOTE: Stay on 0.1.x for now — moving to 0.3.x changes the import paths
# (chains/runnables refactor) and would require a code-level migration.
# Tracked as deferred work in the README.
langchain==0.1.20
langchain-community==0.0.38

# ---------------- LLM client ----------------
ollama==0.4.4

# ---------------- Vector store ----------------
chromadb==0.5.23

# ---------------- Local embeddings ----------------
sentence-transformers>=3.3.0
torch>=2.5.0
transformers>=4.46.0
huggingface-hub>=0.26.0

# ---------------- Document parsing ----------------
# pypdf is the actively maintained successor to PyPDF2 (same maintainer, same API).
pypdf==5.1.0
python-docx==1.1.2

# ---------------- Validation & config ----------------
pydantic==2.10.4
pydantic-settings==2.7.1

# ---------------- HTTP & utilities ----------------
python-dotenv==1.0.1
httpx==0.28.1

# ---------------- Web search (DuckDuckGo) ----------------
ddgs>=3.9.0

# ---------------- Numerical ----------------
# Pinned to 1.x because some upstream deps (transformers/torch ecosystem)
# still ship wheels that mismatch with numpy 2.x in mixed installs.
numpy<2

# ---------------- Persistence ----------------
psycopg2-binary>=2.9.10
sqlalchemy>=2.0.36
alembic>=1.14.0

# ---------------- Tokenizers ----------------
tiktoken>=0.8.0

# ---------------- Caching ----------------
cachetools>=5.5.0

# ---------------- Validation ----------------
email-validator>=2.2.0

# ---------------- Testing ----------------
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
